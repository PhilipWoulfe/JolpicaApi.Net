assembly-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery 
branches:
  master:
    regex: '^master$'
    label: ''
  develop:
    regex: '^develop'
    label: 'alpha'
  feature:
    regex: '^feature[/-]'
    label: 'beta'
  release:
    regex: '^release[/-]'
    label: 'rc'
  bugfix:
    regex: '^bugfix[/-]'
    label: 'bugfix'
ignore:
  sha: []